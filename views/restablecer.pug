<!DOCTYPE html>
html(lang="en")
    head
        include partials/header
    body.login  
        .contenedor-formulario
            h1= nombrePagina
            p Agrega tu Correo para restablecer tu Contraseña
            if locals.mensajes 
                .alertas
                    each categoria in Object.keys( locals.mensajes )
                        each error in mensajes[categoria]
                            .alerta( class = `${categoria}` )
                                p= error
                                    i.fas.fa-exclamation-circle 
            form.caja-login(method = "POST" action = "/restablecer")
                .campo
                    label(for="email") E-mail: 
                    input(type = "text" name = "email" placeholder = "Ingresa tu Email" value= email)
                .campo.enviar
                    input.boton(type = "submit" value = "Restablecer Password")
                .campo-acciones
                    a(href = "/iniciar-sesion") Ya lo recorde
